name: Daily Snyk Agent

on:
  schedule:
    # Runs at 9:00 AM UTC every day
    - cron: "0 9 * * *"
  workflow_dispatch: # Allows manual triggering

jobs:
  run-cn-task:
    runs-on: ubuntu-latest
    # Only run this workflow on the main repository (open-circuit-dev/open-circuit)
    if: github.repository == 'open-circuit-dev/open-circuit'
    steps:
      - name: Checkout repository
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38
        with:
          node-version: "20"

      - name: Install Continue CLI globally
        run: npm install -g @opencircuit/cli@1.5.47

      - name: Run Snyk Agent
        run: cd extensions/cli && cn -p --agent continuedev/snyk-code-scan-agent "The current directory"
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
          OCIRCUIT_API_KEY: ${{ secrets.OCIRCUIT_API_KEY }}
