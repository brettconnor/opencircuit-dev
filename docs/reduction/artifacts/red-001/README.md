# RED-001 Evidence

This directory contains the immutable evidence for the Core lockfile
integrity remediation.

| Directory     | Contents                                                      |
| ------------- | ------------------------------------------------------------- |
| `install/`    | Lockfile generation and clean immutable-install logs          |
| `validation/` | Retained builds, tests, typechecks, and characterization logs |
| `boundaries/` | Static, emitted-bundle, and headless runtime JSON reports     |

The source branch was `red/001-core-lockfile-integrity`, based on
`phase0-cli-core-complete`. The annotated `red-001-core-clean-install`
checkpoint identifies the reviewed final commit.
